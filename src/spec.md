# Specification

## Summary
**Goal:** Publish the current draft build to production.

**Planned changes:**
- Follow the existing production publish checklist in `frontend/PRODUCTION_PUBLISH_V4.md`.
- Complete the verification steps in `frontend/PRODUCTION_SMOKE_CHECK.md`.
- Publish without introducing new product features or user-facing text changes beyond English-only requirements.

**User-visible outcome:** The current draft build is live in production and verified via the existing smoke checks.
